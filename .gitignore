# 运行时与凭据：永不入库
logs/
node_modules/
config/*.local.json
*.local.json
.env

config/stats.json
# 配置备份（.bak）不入库
*.bak
# 开发网关配置（8788，不入库）
scripts/dev.gateway.json
# 下游截断回归测试沙箱（仅本地用，不入库）
scripts/tmp-trunc/
# 探活类残留文件
*.mg_probe.txt

# 本地验证隔离目录（不入库）
.tmp-cfgdir/
